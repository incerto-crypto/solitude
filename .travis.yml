language: python
python:
  - "3.5"
  - "3.6"
install:
  - pip install -U attrs .
  - git config remote.origin.fetch +refs/heads/*:refs/remotes/origin/*
  - git fetch --unshallow --tags
script:
  - pytest -v tests
after_success:
  - bash ci/script.sh 